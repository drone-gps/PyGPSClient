# u-center 经典版 GPS / GNSS 快速上手手册

> 适用软件：u-blox u-center 经典版 25.06（不是 u-center 2）  
> 适用场景：普通 GPS 模块、u-blox 模块、USB GPS、CH340/CP2102/FTDI 转串口、无人机和飞控 GPS 调试。

## 1. 先记住：判断 GPS 是否正常的四个步骤

不要只看“有没有串口数据”，也不要只看“卫星数量”。正确判断顺序是：

1. **通信正常**：COM 口可以打开，数据持续刷新。
2. **卫星信号正常**：能稳定看到多颗卫星，C/N0 不太低。
3. **已经定位**：Fix Mode 从 `No Fix` 变成 `2D Fix`、`3D Fix` 或更高等级。
4. **精度可用**：DOP 和 Accuracy 数值合理，位置不再大幅漂移。

| 步骤 | 重点观察 | 正常表现 | 异常时优先检查 |
|---|---|---|---|
| 通信 | 底部状态栏、Text Console | 显示正确 COM 口和波特率，消息持续更新 | COM 口、波特率、TX/RX、供电、端口占用 |
| 信号 | 卫星柱状图、C/N0 | 室外能看到多颗卫星，多数高于约 25～30 dBHz | 天线方向、遮挡、供电、天线类型 |
| 定位 | Fix Mode、经纬度、卫星数 | 从 `No Fix` 变为 `3D Fix`，经纬度有效 | 移到室外静置，等待冷启动，检查协议输出 |
| 精度 | Accuracy、PDOP、HDOP、位置漂移 | 数值逐渐下降并趋于稳定 | 卫星分布、多路径、干扰、天线位置 |

> **重要区别：**串口有数据只说明通信正常；看到卫星只说明接收机检测到了卫星；只有出现有效 Fix，才表示已经算出位置。

---

## 2. 你的截图当前表示什么

截图中的关键数据是：

- 右上角显示红色的 `No Fix`。
- 当前选中的卫星是 `GPS G18`，即 GPS 星座的 18 号卫星。
- 左侧显示 `El 0.00`、`Az 0.00`，说明该卫星的有效位置参数尚未完整显示或数据尚未更新。
- 信号显示 `C/N0: 15.0 dBHz` 左右。
- 经度、纬度、海拔、精度、DOP 和卫星数等字段尚未产生有效结果。
- 地图、罗盘、速度表和其他仪表没有有效值。

这表示：

1. u-center 可能已经收到部分卫星观测数据。
2. 当前只看到一颗很弱的卫星信号。
3. `15 dBHz` 一般不足以稳定参与定位。
4. GPS 还没有算出有效位置，因此处于 `No Fix`。

你的下一步应该是：

1. 把天线放到室外开阔位置。
2. 白色陶瓷天线面通常朝向天空。
3. 远离墙体、金属、电脑、电机、开关电源和大电流电线。
4. 保持模块静止并等待 5～15 分钟。
5. 观察是否出现多颗卫星、C/N0 是否提高、Fix Mode 是否变成 `3D Fix`。

---

## 3. 如何连接 GPS 模块

### 3.1 确认串口

你的模块之前被 Windows 识别为：

```text
USB-SERIAL CH340 (COM19)
```

在其他电脑上，COM 编号可能变成 `COM3`、`COM7`、`COM12` 等，应以设备管理器实际显示为准。

在 u-center 中选择：

```text
Receiver → Connection → COM19
```

### 3.2 选择波特率

选择：

```text
Receiver → Baudrate → 9600
```

如果没有任何数据，先断开串口，再依次尝试：

```text
9600
38400
57600
115200
230400
```

不要在保持连接时盲目更改波特率。推荐流程是：

1. 断开当前连接。
2. 更改波特率。
3. 重新选择 COM 口。
4. 查看 Text Console 是否有连续数据。

### 3.3 查看原始数据

打开：

```text
View → Text Console
```

如果 GPS 输出 NMEA 文本，应该看到类似内容：

```text
$GNGGA,080531.00,...
$GNRMC,080532.00,A,...
$GNGSA,A,3,...
$GPGSV,4,1,...
```

如果数据持续滚动，说明：

- COM 口基本正确。
- 波特率基本正确。
- GPS 正在向电脑发送数据。

如果全部是乱码，通常是波特率不匹配。

如果完全没有字节，通常是端口、接线、供电或波特率问题，而不只是“没有搜到卫星”。

### 3.4 基本接线

```text
GPS TX  → USB 转串口 RX
GPS RX  → USB 转串口 TX
GPS GND → USB 转串口 GND
```

注意：

- TX 和 RX 要交叉连接。
- 两边必须共地。
- 确认 GPS 供电电压。
- 确认串口逻辑电平，常见为 3.3 V TTL。
- 不要把 TTL 串口直接连接到传统 RS-232 电平接口。

---

## 4. 截图中每块图表是什么意思

| 界面区域 | 代表什么 | 快速读法 |
|---|---|---|
| 顶部圆形天空图 | 卫星在天空中的方位角和仰角 | 中心接近头顶，外圈接近地平线；分布越均匀，通常精度越好 |
| 左侧大号卫星详情 | 当前选中卫星的系统、编号、仰角、方位角和信号 | `GPS G18` 是 GPS 18 号卫星；`El` 是仰角；`Az` 是方位角；`C/N0` 是信号质量 |
| 中部蓝色柱状图 | 当前卫星不同频点或跟踪通道的信号 | 柱越高通常越好；没有柱或柱很低表示未锁定或信号弱 |
| 横向信号历史图 | 一段时间内各颗卫星的 C/N0 变化 | 稳定且持续较好；频繁掉到零说明掉锁、遮挡或干扰 |
| 右上数据面板 | 经纬度、海拔、TTFF、Fix、精度、DOP、卫星数 | 判断 GPS 是否真正可用的核心区域 |
| 右侧罗盘 | 移动方向或航向 | 普通单天线 GPS 静止时没有可靠航向，移动后才有 course over ground |
| 右侧速度表 | 地面速度 | 未定位或静止时接近零；单位取决于软件设置 |
| 右侧时钟类表盘 | GNSS 时间或相关状态 | 接收到有效时间数据后才会正常更新 |
| 下部世界地图 | 当前全球位置及轨迹 | 没有 Fix 时通常没有位置或显示问号 |
| 最下部地图/轨迹窗口 | 更细致的位置和移动轨迹 | 用于观察移动轨迹、静态漂移和位置跳变 |

---

## 5. 卫星天空图怎么看

天空图是一个极坐标图：

- 圆心：头顶正上方，即仰角 90°。
- 外圈：地平线附近，即仰角 0°。
- `N`：北。
- `E`：东。
- `S`：南。
- `W`：西。

### 5.1 Elevation / El：仰角

仰角表示卫星高出地平线多少度：

| 仰角 | 含义 |
|---|---|
| 0° | 位于地平线方向，最容易被建筑、树木和地形遮挡 |
| 30° | 已经高出地平线，通常比低仰角卫星稳定 |
| 60° | 较高，通常信号环境较好 |
| 90° | 位于头顶正上方 |

低仰角卫星更容易受到：

- 建筑遮挡。
- 地面反射。
- 墙面多路径。
- 树木和人体遮挡。

### 5.2 Azimuth / Az：方位角

方位角通常这样定义：

| 方位角 | 方向 |
|---|---|
| 0° | 北 |
| 90° | 东 |
| 180° | 南 |
| 270° | 西 |

### 5.3 卫星不是越多就一定越准

卫星的空间分布也很重要。

例如：

- 8 颗卫星全部集中在天空东侧，几何条件可能仍然不好。
- 6 颗卫星均匀分布在东西南北和不同仰角，几何条件可能更好。

卫星分布不均时，PDOP/HDOP 往往会变大。

---

## 6. 信号柱状图和 C/N0 怎么看

u-center 通常用 `dBHz` 显示 C/N0，即载噪密度比。它表示 GNSS 信号相对于噪声的质量。

它不是普通无线电信号常用的 dBm；不要把二者混为一谈。

| C/N0 | 快速评价 | 常见表现 |
|---:|---|---|
| `< 15 dBHz` | 极弱 | 很难稳定跟踪，通常不能可靠参与定位 |
| `15～25 dBHz` | 弱 | 可能看到卫星，但容易掉锁；室内或窗边常见 |
| `25～35 dBHz` | 基本可用 | 通常可以参与定位，但环境可能仍有遮挡 |
| `35～45 dBHz` | 良好 | 室外常见，定位通常较稳定 |
| `> 45 dBHz` | 很强 | 开阔环境、天线和接收链路状态较好 |

这些数值只是快速工程判断，不同芯片、天线、频点和接收机算法会有差异。

正确的观察方法不是只看最高的一根柱，而是看：

1. 是否同时有多颗卫星。
2. 是否有多颗卫星稳定高于约 25～30 dBHz。
3. 信号是否持续稳定，而不是频繁掉到零。
4. 高信号卫星是否分布在天空不同方向。

你截图中的约 `15 dBHz` 属于弱信号状态，不足以说明接收链路已经正常达到室外使用水平。

---

## 7. Fix Mode 是最重要的状态之一

| Fix 状态 | 含义 | 是否可用 |
|---|---|---|
| `No Fix` | 尚未得到有效位置解 | 不可用于导航或飞行定位 |
| `2D Fix` | 通常得到经纬度，但高度约束不足 | 可粗略平面定位，不建议作为飞控可靠状态 |
| `3D Fix` | 经度、纬度和高度均得到解算 | 普通使用的最低正常目标，还要继续检查精度 |
| `DGPS / DGNSS` | 使用差分改正数据 | 通常比普通单点定位更准 |
| `RTK Float` | 载波相位模糊度还没有完全固定 | 精度一般优于单点定位，但可能发生跳变 |
| `RTK Fixed` | 载波相位模糊度已固定 | 厘米级应用的目标状态，仍要求良好差分链路和卫星环境 |

### 正确判断顺序

1. 先确认串口有正常数据。
2. 再确认能看到多颗卫星。
3. 等待 `3D Fix`。
4. 最后检查 Accuracy、HDOP、PDOP 和位置稳定性。

不要在 `No Fix` 状态下分析经纬度、地图轨迹或速度，因为这些数据此时通常无效。

---

## 8. Accuracy 和 DOP 是什么

### 8.1 Accuracy：接收机估计的误差

右上角可能显示：

- `3D Acc. [m]`：三维位置估计精度。
- `2D Acc. [m]`：水平位置估计精度。

数值通常越小越好。

例如：

```text
2D Acc. = 2.5 m
```

表示接收机估计当前水平位置误差大约处于数米范围，但这只是接收机的估计值，不是绝对保证。

### 8.2 DOP：卫星几何对误差的放大

| 指标 | 含义 |
|---|---|
| PDOP | Position DOP，三维位置几何精度因子 |
| HDOP | Horizontal DOP，水平几何精度因子 |
| VDOP | Vertical DOP，垂直几何精度因子 |
| TDOP | Time DOP，时间几何精度因子 |

DOP 一般越小越好：

| DOP | 工程评价 |
|---:|---|
| `< 1` | 非常理想，实际不总能达到 |
| `1～2` | 很好 |
| `2～5` | 一般可用 |
| `5～10` | 较差，位置可能明显漂移 |
| `> 10` | 很差，不建议依赖 |

注意：

- DOP 只反映卫星几何条件，不直接代表全部误差。
- 即使 HDOP 很低，多路径、天线问题或干扰仍可能造成错误位置。
- 高度通常比水平位置更难估计，因此 VDOP 和高度误差经常更大。

---

## 9. 右上数据面板字段解释

| 字段 | 中文含义 | 使用注意事项 |
|---|---|---|
| Longitude | 经度 | 有 Fix 后才有效；注意东经/西经或正负号 |
| Latitude | 纬度 | 有 Fix 后才有效；注意北纬/南纬或正负号 |
| Altitude | 椭球高 | 相对于参考椭球，不一定等于日常所说的海拔 |
| Altitude (msl) | 平均海平面高 | 更接近日常海拔，但普通 GPS 高度仍可能有数米误差 |
| TTFF | Time To First Fix，首次定位时间 | 冷启动通常较慢，热启动通常较快 |
| Fix Mode | 当前定位类型 | 首先确认是否达到 3D Fix |
| 3D Acc. [m] | 三维位置估计精度 | 越小越好 |
| 2D Acc. [m] | 水平位置估计精度 | 越小越好，地图位置最常关注这一项 |
| PDOP | 三维几何精度因子 | 越小越好 |
| HDOP | 水平几何精度因子 | 越小越好 |
| Satellites | 卫星数量 | 要区分可见、正在跟踪和实际参与解算的卫星 |

### Altitude 和 Altitude (msl) 为什么不同

- `Altitude` 常指椭球高。
- `Altitude (msl)` 常指相对于平均海平面的高度。
- 二者之间存在大地水准面差异，所以数值不同属于正常现象。

---

## 10. 右侧罗盘、速度表和时钟为什么没有数据

### 10.1 罗盘/方向图

普通单天线 GPS 通常不能在静止状态下测得机头朝向。

它显示的往往是移动方向，即 Course Over Ground：

- 载体移动后才会逐渐稳定。
- 静止时方向可能为空、抖动或随机变化。
- 它不等同于磁罗盘航向。
- 双天线 GNSS 定向系统是另一种情况，可以在静止时测航向。

### 10.2 速度表

速度表表示地面速度：

- 没有 Fix 时通常无效。
- 静止时应接近零，但可能有小幅噪声。
- 单位可能为 km/h、m/s 或 knots，取决于软件设置。

### 10.3 时间/时钟

GNSS 可以提供高精度 UTC 时间。即使位置还没有完全定位，有些接收机也可能先获得有效时间。

如果时间一直无效，应检查：

- 是否真正收到导航消息。
- 当前数据输出是否包含时间字段。
- 卫星信号是否足够。

---

## 11. 常见卫星系统和标识

| 标识 | 卫星系统 | 说明 |
|---|---|---|
| GPS / Gxx | 美国 GPS | 截图中的 `GPS G18` 是 GPS 18 号卫星 |
| GLO / Rxx | 俄罗斯 GLONASS | 多星座接收可以提高可用性 |
| GAL / Exx | 欧洲 Galileo | 现代信号和多频能力较好 |
| BDS / Cxx | 中国北斗 | 中国及亚太地区通常可见度良好 |
| QZSS / Jxx | 日本准天顶系统 | 可改善亚太城市环境中的可用性 |
| SBAS / Sxx | 星基增强系统 | 提供改正或完整性信息，覆盖能力取决于地区 |

同时启用多个星座通常可以：

- 增加可见卫星数量。
- 缩短首次定位时间。
- 改善建筑遮挡环境下的可用性。
- 改善卫星几何分布。

但同时也会增加接收机的配置和功耗复杂度。

---

## 12. Text Console 中常见的 NMEA 消息

| NMEA 消息 | 包含内容 | 主要用途 |
|---|---|---|
| GGA | 时间、经纬度、Fix 质量、卫星数、海拔 | 快速确认是否定位 |
| RMC | 时间、状态、位置、速度、航向、日期 | 导航应用中最常用的最小数据集 |
| GSA | 参与解算的卫星和 DOP | 判断哪些卫星参与解算及几何质量 |
| GSV | 可见卫星、仰角、方位角、信号强度 | 对应天空图和信号柱 |
| VTG | 地面航向和速度 | 移动载体的速度与方向 |
| GLL | 经纬度和时间 | 简化的位置数据 |

### GGA 中经常关注的内容

一条 GGA 示例可能类似：

```text
$GNGGA,083120.00,3112.34567,N,12128.76543,E,1,12,0.9,15.2,M,...
```

常见字段含义包括：

- UTC 时间。
- 纬度。
- 南北半球。
- 经度。
- 东西半球。
- Fix 质量。
- 参与定位的卫星数量。
- HDOP。
- 海拔。

### RMC 中的状态字符

RMC 常包含：

- `A`：数据有效。
- `V`：数据无效。

即使串口持续输出 RMC，如果状态为 `V`，也不能认为已经获得有效位置。

---

## 13. Messages View 中常见的 u-blox UBX 消息

打开：

```text
View → Messages View
```

常见重要消息：

| UBX 消息 | 包含内容 | 主要用途 |
|---|---|---|
| NAV-PVT | 位置、速度、时间、Fix、卫星数、精度 | 最重要的综合导航消息 |
| NAV-SAT | 每颗卫星的信号、方位、仰角和使用状态 | 诊断卫星和天线质量 |
| NAV-DOP | PDOP、HDOP、VDOP 等 | 判断卫星几何质量 |
| NAV-STATUS | 当前导航和定位状态 | 确认 Fix 状态 |
| MON-VER | 硬件、固件和协议版本 | 确认模块型号和能力 |
| CFG-PRT | 串口端口和协议配置 | 检查 UART 波特率及输入/输出协议 |
| CFG-RATE | 测量周期和导航更新率 | 检查 1 Hz、5 Hz、10 Hz 等输出频率 |

对于新手，优先看：

```text
UBX → NAV → PVT
UBX → NAV → SAT
UBX → NAV → DOP
UBX → MON → VER
```

---

## 14. 为什么室内经常 No Fix

GNSS 卫星距离地面很远，到达接收机的信号非常微弱。以下物体都会显著衰减或反射信号：

- 钢筋混凝土屋顶。
- 金属屋顶和金属外壳。
- 镀膜玻璃。
- 建筑墙面。
- 树木和潮湿树叶。
- 人体。
- 车辆内部。

室内或窗边经常出现：

- 只能看到一两颗卫星。
- C/N0 只有 10～25 dBHz。
- 卫星信号时有时无。
- 一直处于 No Fix。
- 偶尔定位，但位置漂移很大。

这不一定说明模块损坏。判断硬件性能时应进行室外开阔环境测试。

---

## 15. 首次室外测试 SOP

建议严格按照下面顺序进行：

1. 关闭其他可能占用串口的软件，只保留一个 u-center。
2. 选择正确 COM 口和波特率。
3. 在 Text Console 中确认消息持续刷新。
4. 把天线移到室外开阔位置。
5. 陶瓷天线白色面通常朝向天空。
6. 天线远离电脑、显示器、电机、VTX、开关电源和大电流电线。
7. 保持模块静止，不要拿在手里反复移动。
8. 首次冷启动等待 5～15 分钟，必要时等待更久。
9. 观察是否出现多颗超过约 25～30 dBHz 的卫星。
10. 确认 Fix Mode 变为 `3D Fix`。
11. 等待 HDOP、PDOP 和 Accuracy 逐渐下降并稳定。
12. 记录卫星数、C/N0、Fix、HDOP、位置精度和首次定位时间。

### 一个基本正常的室外结果示例

```text
Fix Mode: 3D Fix
Satellites used: 10
Strong satellites: 多颗达到 35～45 dBHz
HDOP: 1.2
PDOP: 1.8
Horizontal accuracy estimate: 2～4 m
Position: 静止后相对稳定
```

不同模块和环境会有差异，这只是示例，不是强制标准。

---

## 16. 常见故障现象和处理方法

| 现象 | 可能原因 | 处理方法 |
|---|---|---|
| u-center 中没有 COM19 | 驱动未安装、USB 线问题、模块未供电 | 查看设备管理器，重新插拔，更换 USB 线或端口 |
| COM19 无法打开 | 被其他软件占用 | 关闭 PyGPSClient、u-center 2、串口助手、Arduino 串口监视器等 |
| Text Console 完全没数据 | 波特率错误、TX/RX 接错、没有共地、模块未供电 | 依次检查串口、接线、供电和波特率 |
| Text Console 全是乱码 | 波特率不匹配 | 断开后尝试其他常见波特率 |
| 有 NMEA 数据但状态无效 | 还没有 Fix | 移到室外等待，检查信号柱和卫星数 |
| 能看到卫星但只有一两颗 | 室内遮挡、天线方向错误、天线性能差 | 移到室外，调整天线朝向 |
| 多颗卫星但一直 No Fix | 信号仍弱、数据不完整、星历未取得、模块配置异常 | 室外静置冷启动，检查 NAV-PVT/GGA/GSA |
| 已定位但位置乱跳 | 多路径、DOP 高、信号弱、干扰 | 远离建筑和干扰源，改善天线位置 |
| u-center 能用但飞控读不到 | 飞控 UART、协议、波特率或消息配置不一致 | 检查飞控串口配置和 GPS 输出协议 |
| 飞控能用但 u-center 读不到 | USB 转串口接线或波特率与飞控配置不同 | 查询飞控使用的 GPS 波特率和协议 |

---

## 17. 无人机和飞控使用 GPS 的额外注意事项

### 17.1 安装位置

- GPS 天线尽量安装在机体上方。
- 远离 VTX、图传天线、ELRS/遥控发射源。
- 远离电调、电机线、电池线和 DC-DC 电源。
- 尽量不要被碳纤维板或金属件遮挡。
- GPS 下方适当使用接地金属面可能有助于天线性能，但要遵循模块厂商设计。

### 17.2 GPS 与罗盘不是一回事

一些无人机 GPS 模块集成磁罗盘：

- GPS 负责位置、速度和时间。
- 磁罗盘负责机头方向。
- GPS 搜星正常不代表罗盘没有磁干扰。
- 罗盘应远离电池线、电机和大电流导体。

### 17.3 飞行前不要只看卫星数量

更完整的检查应包括：

- 是否达到 3D Fix。
- 参与解算的卫星数量是否足够。
- HDOP/PDOP 是否满足飞控要求。
- 位置是否稳定。
- Home Point 是否正确建立。
- 罗盘方向是否正确。
- GPS 数据是否持续更新。

### 17.4 不要随意保存永久配置

在 u-center 中修改以下配置时应非常谨慎：

- UART 波特率。
- UART 输入协议。
- UART 输出协议。
- NMEA/UBX 消息开关。
- 导航更新频率。
- 动态模型。
- GNSS 星座配置。

错误配置可能导致：

- u-center 可以连接，但飞控无法读取。
- 飞控可以读取，但 u-center 用默认波特率无法连接。
- 只输出 UBX，不再输出 NMEA。
- 只输出 NMEA，无法看到需要的 UBX 消息。
- 串口数据量超过当前波特率承载能力。

修改前建议记录或截图原配置。

---

## 18. 什么状态可以认为“基本正常”

| 检查项 | 最低可接受状态 | 较好状态 |
|---|---|---|
| 串口通信 | 消息连续，无频繁解析错误 | NMEA/UBX 消息稳定，无掉线 |
| Fix | 3D Fix | 稳定的 3D/DGNSS/RTK 状态 |
| 参与解算卫星 | 理论至少 4 颗，实际建议更多 | 8～15 颗以上且分布较均匀 |
| C/N0 | 多颗达到约 25～30 dBHz | 多颗达到 35～45 dBHz |
| HDOP | 小于 5 | 约 1～2 |
| PDOP | 小于 5 | 约 1～2.5 |
| 水平精度估计 | 满足应用需求 | 开阔环境普通单点定位常见数米量级 |
| 静态稳定性 | 位置没有大幅跳变 | 位置、卫星数、DOP 和 C/N0 相对稳定 |

以上是快速工程判断，并不是测绘、航空或安全认证标准。

---

## 19. 推荐的故障排查顺序

不要跳着排查，应按下面顺序逐层确认：

1. **电脑是否识别串口？** 设备管理器能看到 COM 口吗？
2. **串口是否能打开？** 是否被其他软件占用？
3. **波特率是否正确？** Text Console 能显示连续、可解析的消息吗？
4. **天线是否工作？** 移到室外后，卫星数和 C/N0 是否明显提高？
5. **是否获得 3D Fix？** 经纬度和时间是否有效？
6. **精度是否足够？** DOP 和 Accuracy 是否合理并稳定？
7. **飞控是否能读取？** UART、协议、波特率和消息输出是否匹配？

只要前一步没有确认，就不要急着排查下一步。

---

## 20. 你的当前状态与下一步操作

根据截图，目前属于：

```text
串口/协议：可能已经收到部分数据
可见卫星：至少识别到 GPS G18
C/N0：约 15 dBHz，信号很弱
Fix Mode：No Fix
位置：无有效结果
```

建议立即执行：

1. 确认 u-center 底部显示正确的 COM 口和波特率。
2. 打开 Text Console，确认数据持续更新且不是乱码。
3. 将 GPS 天线移到室外开阔位置。
4. 白色陶瓷天线面朝天，模块保持静止。
5. 等待 5～15 分钟。
6. 观察是否出现多颗 25～30 dBHz 以上的卫星。
7. 等待 Fix Mode 变为 `3D Fix`。
8. 再检查 HDOP、PDOP、Accuracy、经纬度和地图位置。

> **当前最重要的目标不是研究地图、罗盘或速度表，而是先让多颗卫星的信号变强，并让 Fix Mode 从 `No Fix` 变为 `3D Fix`。**

---

## 21. 快速测试记录模板

可以复制下面的内容记录每次测试：

```text
测试日期：
测试地点：室内 / 窗边 / 室外开阔处
模块型号：
天线类型：内置陶瓷 / 外置无源 / 外置有源
供电电压：
COM 端口：
波特率：
输出协议：NMEA / UBX / 两者都有
首次定位时间 TTFF：
Fix Mode：
可见卫星数：
参与解算卫星数：
最高 C/N0：
超过 30 dBHz 的卫星数：
HDOP：
PDOP：
2D Accuracy：
3D Accuracy：
经纬度是否稳定：
是否存在频繁掉星：
其他现象：
```

---

## 22. 最短记忆版

以后每次打开 u-center，只需要按这个顺序看：

```text
COM 口有数据吗？
        ↓
有多颗卫星吗？
        ↓
C/N0 是否多数超过 25～30 dBHz？
        ↓
Fix 是否达到 3D？
        ↓
HDOP / PDOP / Accuracy 是否合理？
        ↓
静止位置是否稳定？
```

如果全部满足，GPS 才算基本进入可用状态。
