# u-center 经典版：10 个图表窗口位置与含义

> 适用软件：u-blox u-center 经典版 25.06。  
> 本文按用户提供的截图布局编号。u-center 的窗口可以拖动、停靠、关闭和重新排列，所以其他电脑上的实际位置可能不同，但窗口名称和用途相同。

## 1. 截图位置编号总览

截图中一共有 10 个图表或数据窗口，分成左侧 5 个和右侧 5 个：

```text
┌───────────────────────────────┬───────────────┐
│ 左1：Satellite Position       │ 右1：Data     │
├───────────────────────────────┼───────────────┤
│                               │ 右2：Compass  │
│ 左2：Satellite Level          ├───────────────┤
│                               │ 右3：Speed    │
├───────────────────────────────┼───────────────┤
│ 左3：Satellite Level History  │ 右4：Altitude │
├───────────────────────────────┼───────────────┤
│ 左4：World Position           │ 右5：Watch    │
├───────────────────────────────┤               │
│ 左5：Map View                 │               │
└───────────────────────────────┴───────────────┘
```

对应关系：

| 位置编号 | u-center 官方英文名称 | 中文名称 | 是否对应红框工具栏按钮 |
|---|---|---|---|
| 左1 | Satellite Position | 卫星位置图 | 是 |
| 左2 | Satellite Level | 卫星信号强度图 | 是 |
| 左3 | Satellite Level History | 卫星信号强度历史图 | 是 |
| 左4 | World Position | 世界位置概览 | 是 |
| 左5 | Map View | 地图视图 | 否，是额外打开的普通 View 窗口 |
| 右1 | Data | 导航数据摘要 | 是 |
| 右2 | Compass | 罗盘/运动方向表 | 是 |
| 右3 | Speed Meter | 速度表 | 是 |
| 右4 | Altitude Meter | 高度表 | 是 |
| 右5 | Watch | GNSS 时钟 | 是 |

红框中的 9 个按钮用于显示或隐藏 9 个 Docking Windows。左5 `Map View` 不属于这 9 个 Docking Windows，需要通过 View 菜单单独打开。

常用菜单路径：

```text
View → Docking Windows → 对应窗口名称
```

地图视图通常从 View 菜单的 Map View 项打开。

---

## 2. 左1：Satellite Position - 卫星位置图

### 在截图中的位置

左侧最上方的窄窗口，中间有一个带 `N / E / S / W` 的圆形天空图。

### 它表示什么

这个窗口显示当前接收机所观察到的卫星在天空中的位置。

圆图的含义：

- 圆心：头顶正上方，即仰角 90°。
- 外圈：地平线附近，即仰角 0°。
- `N`：北。
- `E`：东。
- `S`：南。
- `W`：西。
- 卫星标记：每颗已检测卫星的编号和天空位置。

### 两个关键角度

| 参数 | 英文 | 含义 |
|---|---|---|
| 仰角 | Elevation / El | 卫星高出地平线的角度，0° 在地平线，90° 在头顶 |
| 方位角 | Azimuth / Az | 卫星所在水平方向，北 0°、东 90°、南 180°、西 270° |

### 怎么判断

正常室外环境应逐渐看到多颗卫星分布在天空不同方向。

- 卫星越靠近圆心，仰角越高。
- 卫星越靠近外圈，仰角越低，更容易被建筑和地面反射影响。
- 卫星均匀分布在东西南北，通常比全部集中在一个方向更有利于定位精度。
- 图中只有一两颗卫星时，一般很难获得稳定 3D Fix。

### 什么时候会是空的

- GPS 模块没有输出卫星状态消息。
- 串口或波特率不正确。
- 天线没有接好。
- 当前在室内或严重遮挡环境。
- 接收机刚启动，还没有获得卫星信息。

---

## 3. 左2：Satellite Level - 卫星信号强度图

### 在截图中的位置

左侧第二个、面积最大的黑色窗口。收到数据后，这里通常会出现多根不同颜色的卫星信号柱。

### 它表示什么

它显示当前各颗卫星的接收信号强度，通常使用 C/N0，单位为 dBHz。

每根柱通常对应一颗卫星或某颗卫星的一个信号频点。柱子的标签可能出现：

```text
G18
E11
B23
R05
```

其中：

- `Gxx`：GPS。
- `Exx`：Galileo。
- `Bxx`：BeiDou/北斗。
- `Rxx`：GLONASS。
- `Sxx`：SBAS。
- `Qxx`：QZSS。

### C/N0 快速判断

| C/N0 | 大致状态 | 典型含义 |
|---:|---|---|
| 小于 15 dBHz | 极弱 | 通常难以稳定跟踪 |
| 15～25 dBHz | 弱 | 可能看到卫星，但容易掉锁 |
| 25～35 dBHz | 基本可用 | 通常可以参与定位 |
| 35～45 dBHz | 良好 | 室外正常使用常见范围 |
| 大于 45 dBHz | 很强 | 天线和环境通常较好 |

### 怎么判断

不要只看最高的一根柱。更重要的是：

1. 是否同时有多颗卫星。
2. 是否有多颗卫星超过约 25～30 dBHz。
3. 信号柱是否稳定，而不是频繁消失。
4. 高信号卫星是否分布在天空不同方向。

如果这个大窗口完全空白，应先检查 Text Console 是否有数据，而不是只等待搜星。

---

## 4. 左3：Satellite Level History - 卫星信号强度历史图

### 在截图中的位置

左侧第三个横向窗口，右侧纵轴标有约 `10、20、30、40、50 dB`。

### 它表示什么

左2显示“当前这一刻”的信号强度；左3显示“一段时间内”的信号强度变化。

横轴通常代表时间或连续历元，纵轴代表 C/N0。每条彩色轨迹对应一颗卫星或一个信号。

### 它最适合发现什么问题

- 卫星是否频繁掉锁。
- 信号是否周期性变弱。
- 天线或接头是否接触不良。
- 遮挡是否随载体移动而变化。
- 电机、图传或开关电源是否造成干扰。
- 无人机通电前后 GNSS 信号是否明显下降。

### 怎么判断

| 历史曲线表现 | 可能含义 |
|---|---|
| 多条曲线稳定在较高位置 | 接收环境较稳定 |
| 曲线缓慢升高 | 模块启动后逐渐锁定卫星 |
| 单颗卫星偶尔下降 | 卫星运动或局部遮挡，可能正常 |
| 多颗卫星同时突然下降 | 天线遮挡、供电波动或宽带干扰 |
| 曲线频繁掉到零再恢复 | 掉锁、接触不良、严重干扰或环境遮挡 |

对于无人机，建议比较以下状态：

```text
仅 GPS 上电
飞控上电
图传上电
电机怠速
电机提高油门
```

如果某个设备开启后多颗卫星的 C/N0 同时下降，通常说明存在电磁干扰。

---

## 5. 左4：World Position - 世界位置概览

### 在截图中的位置

左侧第四个较矮窗口，背景是扁平的世界地图，左下角显示 `Longitude` 和 `Latitude`，右下角可能显示问号。

### 它表示什么

这是一个轻量级的全球位置概览 Docking Window，用于快速显示当前位置位于世界地图的什么区域。

它主要回答：

```text
接收机现在大概位于世界上的哪里？
```

有有效定位后，地图上会显示当前位置标记。没有 Fix 时，经纬度通常为空或显示问号。

### 它适合做什么

- 快速检查经纬度是否在正确国家或地区。
- 发现经纬度正负号错误。
- 发现南北纬、东西经解释错误。
- 粗略判断日志记录位置。

### 它不适合做什么

- 不适合查看街道级细节。
- 不适合精确测距。
- 不适合评估米级漂移。
- 不适合作为导航地图。

---

## 6. 左5：Map View - 地图视图

### 在截图中的位置

左侧最下方的大窗口，背景同样有世界地图，但面积更大、经纬网格更详细。

### 它为什么不在红框 9 个按钮中

左5不是 Docking Window，而是一个独立的普通 View 窗口。它和左4的 `World Position` 不属于同一类窗口。

官方说明中，Map View 可以显示：

- 预先校准的本地地图。
- 在线动态地图。
- GNSS 位置点。
- 移动轨迹。
- 日志回放的位置轨迹。

### 左4和左5的区别

| 对比项 | 左4 World Position | 左5 Map View |
|---|---|---|
| 类型 | Docking Window | 普通 View 窗口 |
| 主要目的 | 快速看全球位置 | 查看位置点和轨迹 |
| 交互能力 | 较简单 | 更强，可加载、缩放或校准地图 |
| 适用精度 | 国家/区域级快速确认 | 轨迹分析和更细的位置观察 |
| 工具栏红框按钮 | 有 | 没有 |

### 怎么使用

有 3D Fix 后，当前位置会出现在地图上。移动 GPS 时，地图可以显示轨迹。

它适合：

- 步行或车辆测试。
- 查看日志回放轨迹。
- 判断位置跳点。
- 比较不同天线或配置下的轨迹稳定性。

### 注意

u-center 的地图只是测试和可视化工具，不应作为实时安全导航系统使用。

---

## 7. 右1：Data - 导航数据摘要

### 在截图中的位置

右侧最上方的表格窗口，包含：

```text
Longitude
Latitude
Altitude
Altitude (msl)
TTFF
Fix Mode
3D Acc. [m]
2D Acc. [m]
PDOP
HDOP
Satellites
```

### 它表示什么

这是判断 GPS 是否真正进入可用状态的核心窗口。它集中显示当前导航解算结果。

### 字段解释

| 字段 | 含义 | 怎么看 |
|---|---|---|
| Longitude | 经度 | 有有效 Fix 后才可信 |
| Latitude | 纬度 | 有有效 Fix 后才可信 |
| Altitude | 椭球高 | 相对于参考椭球，不一定等于日常海拔 |
| Altitude (msl) | 平均海平面高 | 更接近日常所说的海拔 |
| TTFF | 首次定位时间 | 从启动到第一次有效 Fix 所花时间 |
| Fix Mode | 定位类型 | `No Fix`、`2D Fix`、`3D Fix`、DGNSS、RTK 等 |
| 3D Acc. [m] | 三维位置估计精度 | 越小通常越好 |
| 2D Acc. [m] | 水平位置估计精度 | 判断地图平面位置时重点看 |
| PDOP | 三维几何精度因子 | 越小通常越好 |
| HDOP | 水平几何精度因子 | 越小通常越好 |
| Satellites | 卫星数量 | 要注意可见、跟踪和参与解算并不完全相同 |

### 最重要的判断顺序

```text
Fix Mode 是否为 3D Fix？
        ↓
经纬度是否有效？
        ↓
参与解算卫星是否足够？
        ↓
2D/3D Accuracy 是否合理？
        ↓
PDOP/HDOP 是否较小且稳定？
```

截图中显示红色 `No Fix`，因此其他位置、速度、高度和地图数据尚不能作为有效结果使用。

---

## 8. 右2：Compass - 罗盘/运动方向表

### 在截图中的位置

右侧第二个窗口，中央显示一个八角罗盘星形图案。

### 它表示什么

它通常显示 GNSS 计算得到的地面运动方向，即 Course Over Ground，而不一定是磁罗盘的机头朝向。

### 新手最容易误解的地方

普通单天线 GPS 在静止时，无法可靠知道“设备朝向哪里”。

原因是普通 GPS 主要根据连续位置变化计算运动方向：

- 设备向东移动，方向接近 90°。
- 设备向南移动，方向接近 180°。
- 设备静止时，微小位置噪声会使方向不稳定或无效。

因此：

- 静止时罗盘为空或乱跳，不一定是故障。
- 移动达到一定速度后，方向才更可靠。
- 它不等同于飞控的磁罗盘航向。
- 双天线 GNSS 定向模块可以在静止时测航向，但那是不同原理。

---

## 9. 右3：Speed Meter - 速度表

### 在截图中的位置

右侧第三个窗口，圆形刻度大约从 `0` 到 `250`。

### 它表示什么

它显示 GNSS 解算得到的地面速度。

速度通常来自：

- 连续位置变化。
- 接收机的多普勒测速结果。

### 怎么看

- 没有 Fix 时，读数通常无效或保持在零附近。
- GPS 静止时应接近零，但可能有少量噪声。
- 移动后指针会随地面速度变化。
- 显示单位取决于 u-center 的设置，可能是 km/h、m/s 或 knots。

### 它不代表什么

- 不代表垂直爬升速度。
- 不代表空速。
- 不代表电机转速。
- 对无人机来说，它通常是相对于地面的速度，不是相对于空气的速度。

---

## 10. 右4：Altitude Meter - 高度表

### 在截图中的位置

右侧第四个窗口，圆盘周围显示 `0～9` 的循环刻度，看起来类似航空机械高度表。

### 它表示什么

它以指针表盘形式显示 GNSS 高度。

表盘设计类似机械高度表，通常通过不同指针或转数表达不同数量级。由于截图处于 `No Fix`，当前指针没有有效高度意义。

### GPS 高度为什么容易误解

GPS 中可能同时存在两种高度：

| 高度类型 | 含义 |
|---|---|
| Ellipsoid Height | 相对于参考椭球的高度 |
| MSL Height | 相对于平均海平面的高度，更接近日常海拔 |

二者可能相差几十米，这不一定是模块错误，而是参考基准不同。

### 使用注意事项

- 普通单点 GNSS 的垂直精度通常比水平精度差。
- 高度在刚获得 Fix 时可能继续变化。
- 不要把普通 GPS 高度当成精密测高结果。
- 无人机通常还会结合气压计获得更平滑的相对高度。

---

## 11. 右5：Watch - GNSS 时钟

### 在截图中的位置

右侧最下方的圆形表盘，外观像机械时钟。

### 它表示什么

它显示接收机提供的 GNSS 时间，通常以 UTC 为基准。

GNSS 卫星携带高精度原子钟，接收机可以从卫星导航消息中恢复准确时间。

### 怎么看

- 接收机获得有效时间后，指针会显示当前时间。
- u-center 底部状态栏也可能显示 NMEA 时间和 `UTC`。
- GNSS 时间通常是 UTC，不一定自动转换为北京时间。

北京时间与 UTC 的关系：

```text
北京时间 = UTC + 8 小时
```

### 为什么有时位置还没定位，时间已经有效

接收机获得时间所需的信息可能少于完整三维定位，因此可能出现：

```text
时间已经更新，但 Fix 仍然是 No Fix
```

这并不矛盾。

---

## 12. 红框 9 个工具栏按钮的含义

截图红框中的 9 个按钮用于快速打开或关闭以下 9 个 Docking Windows：

1. Satellite Position。
2. Satellite Level。
3. Satellite Level History。
4. World Position。
5. Data。
6. Altitude Meter。
7. Compass。
8. Speed Meter。
9. Watch。

不同版本或工具栏自定义状态下，按钮的显示顺序可能略有差异。最可靠的确认方法是：

```text
View → Docking Windows
```

菜单项前有勾号，表示该窗口当前已显示。

点击工具栏按钮只是在显示和隐藏窗口，不会改变 GPS 模块配置，也不会影响串口输出。

---

## 13. 为什么有些窗口现在是空的

你的截图底部显示：

```text
COM19 38400
```

这说明 u-center 当前选择了 COM19 和 38400 波特率，但界面右1仍显示红色 `No Fix`，大量窗口为空。

可能有三种情况：

### 情况 A：串口已连接，但 GPS 还没有定位

表现：

- Text Console 有正常 NMEA/UBX 数据。
- 能逐渐看到卫星。
- Fix Mode 暂时为 No Fix。

处理：移到室外开阔处静置等待。

### 情况 B：COM 口打开了，但波特率或协议不正确

表现：

- Text Console 没数据或全是乱码。
- 所有图表一直为空。
- 卫星位置和信号柱不更新。

处理：断开后检查 9600、38400、57600、115200 等波特率，并确认模块输出 NMEA 或 UBX。

### 情况 C：GPS 数据类型不包含图表所需消息

表现：

- Text Console 有部分数据。
- 经纬度可能有效。
- 但卫星位置或信号图仍为空。

原因可能是模块只输出 RMC/GGA，没有输出 GSV/GSA 或相应 UBX 卫星状态消息。

例如：

- `GGA/RMC` 主要提供位置、时间、速度。
- `GSV` 提供可见卫星、仰角、方位角和信号强度。
- `GSA` 提供参与解算卫星和 DOP。
- `UBX-NAV-SAT` 提供详细卫星状态。
- `UBX-NAV-PVT` 提供综合位置、速度、时间和精度。

---

## 14. 建议新手重点保留哪些窗口

如果觉得 10 个窗口太多，可以先只保留 5 个最有用的：

| 优先级 | 窗口 | 用途 |
|---|---|---|
| 1 | 右1 Data | 看 Fix、经纬度、卫星数、Accuracy 和 DOP |
| 2 | 左2 Satellite Level | 看当前卫星信号是否足够 |
| 3 | 左1 Satellite Position | 看卫星天空分布是否合理 |
| 4 | 左3 Satellite Level History | 看是否掉星或受干扰 |
| 5 | 左5 Map View | 看位置和移动轨迹 |

Compass、Speed、Altitude 和 Watch 是直观仪表，但诊断 GPS 是否正常时，它们的重要性低于 Data、Satellite Level 和 Satellite Position。

---

## 15. 10 个窗口的最短记忆法

```text
左1：卫星在哪里
左2：卫星现在有多强
左3：卫星信号过去是否稳定
左4：我大概在世界哪里
左5：我的位置和轨迹在哪里

右1：当前导航结果和精度
右2：我正在往哪个方向移动
右3：我移动得多快
右4：我有多高
右5：现在的 GNSS 时间
```

真正判断 GPS 是否可用时，建议按下面顺序查看：

```text
左2 信号强度
    ↓
左1 卫星分布
    ↓
右1 Fix / 卫星数 / Accuracy / DOP
    ↓
左3 信号稳定性
    ↓
左5 位置与轨迹
```

不要首先依赖罗盘、速度表和高度表。没有有效 3D Fix 时，这些仪表通常没有可靠意义。

---

## 16. 官方资料依据

本文窗口名称和用途依据本机 u-center 25.06 安装目录内的官方用户手册：

```text
C:\Program Files (x86)\u-blox\u-center_v25.06\u-center_UserGuide_UBX-13005250.pdf
```

官方手册名称：

```text
u-center - User guide
Document number: UBX-13005250
Revision: R30
```

官方手册将以下 9 个窗口归类为 Docking Windows：

- Satellite Position。
- Satellite Level。
- Satellite Level History。
- World Position。
- Data。
- Altitude Meter。
- Compass。
- Speed Meter。
- Watch。

Map View 则属于独立的图形 View，用于在预校准地图或在线动态地图上显示位置和轨迹。
